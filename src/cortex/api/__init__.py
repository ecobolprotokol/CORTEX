"""CORTEX embedded API - HTTP server, routes, handlers.

This package implements the embedded HTTP API server,
including route definitions, authentication, and
request handlers.

Key components:
- HTTP server (Starlette + Uvicorn)
- Route definitions
- Authentication (Bearer token)
- Request handlers

Governing Doc: DOC-05
"""
