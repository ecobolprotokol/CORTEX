"""CORTEX CLI - command parsing, dispatch.

This package implements the command-line interface,
including argument parsing and command dispatch.

Key components:
- CLI argument parsing (Click)
- Command dispatch
- Command implementations

Governing Doc: DOC-05
"""
